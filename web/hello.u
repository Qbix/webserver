// web/hello.u — GET /hello
// Drop this file in web/ and it handles that URL path.

f handle(req: Request) -> Response
	name = req.query.get("name") ?? "World"
	r => Response.html("<h1>Hello, " + name + "!</h1><p>This is a U handler.</p>")
